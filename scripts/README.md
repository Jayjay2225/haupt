# scripts/

Wartungs- und Datenpflege-Skripte.

- `update-insurers.ts` (entsteht mit Prompt 2): trägt neue Jahrgänge in `data/insurers.json` nach, prüft Plausibilität und pflegt die Änderungshistorie (`data.version`).
